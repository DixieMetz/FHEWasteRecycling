#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Running pre-commit checks..."

# Run linting
echo "📝 Checking Solidity code quality..."
npm run lint || {
  echo "❌ Solidity linting failed. Please fix the errors and try again."
  exit 1
}

# Run JavaScript linting
echo "📝 Checking JavaScript code quality..."
npm run lint:js || {
  echo "❌ JavaScript linting failed. Please fix the errors and try again."
  exit 1
}

# Check formatting
echo "🎨 Checking code formatting..."
npm run format:check || {
  echo "❌ Code formatting check failed. Run 'npm run format' to fix."
  exit 1
}

# Run tests
echo "🧪 Running tests..."
npm run test || {
  echo "❌ Tests failed. Please fix the failing tests and try again."
  exit 1
}

echo "✅ All pre-commit checks passed!"
